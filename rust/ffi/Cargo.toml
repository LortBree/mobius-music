[package]
name = "offline-player-ffi"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
offline-player-app-core = { path = "../app-core" }