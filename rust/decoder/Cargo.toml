[package]
name = "offline-player-decoder"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
symphonia = { version = "0.6.1", default-features = false, features = ["flac", "wav"] }
thiserror = "2"
sha2 = "0.10"
hex = "0.4"
offline-player-audio-core = { path = "../audio-core" }